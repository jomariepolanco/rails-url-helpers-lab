<h1>Student Page for </h1>

<!-- <h1><%= @student.first_name + " " + @student.last_name %></h1> -->
<h1><%= @student.to_s %></h1>

<% if @student.active %>
    This student is currently active.
<% elsif @student.active == false %>
    This student is currently inactive.
<% end %>